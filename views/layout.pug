doctype
head
	title= page
	meta(charset='utf-8')
	meta(name='viewport', content='width=device-width, initial-scale=1.0')
	link(rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css')
	script(src='/front.js')
	style
		|ul.nav a {
		|	text-transform: capitalize;
		|}
body
	.container
		h1= page
		hr
		ul.nav.nav-pills
			each link in links
				li: a.nav-link(href=link)= link
			li: a.nav-link(href='/new') new
		hr
		block content